imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: "@SiteBundle/Resources/config/services.yml" }

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: fr
    site_url: http://new.lademoisaile.ca/web/app_dev.php

framework:
    #esi: ~
    translator: { fallbacks: ['%locale%'] }
#    translator: ~
    secret: '%secret%'
    router:
        resource: '%kernel.project_dir%/app/config/routing.yml'
        strict_requirements: ~
    form: ~
    csrf_protection: ~
    validation: { enable_annotations: true }
    #serializer: { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale: '%locale%'
    trusted_hosts: ~
    session:
        # https://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id: session.handler.native_file
        save_path: '%kernel.project_dir%/var/sessions/%kernel.environment%'
    fragments: ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    form_themes: ['bootstrap_4_layout.html.twig']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    date:
        timezone:  America/New_York
    globals:
        ga_tracking: UA-78069695-1
        webmaster: <a href="https://viweb.ca" title="ViWeb - Agence web" target="_blank">ViWeb</a>
        compagnie: La deMOIs’aile
        description: Paysagiste
        type_compagnie: Paysagiste
        adresse_civique: 1206 Rue de l'Orée des Bois
        ville: Sainte-Adèle
        code_postal: J8B 1B4
        province: Québec
        telephone: (450) 660-0131
        fax:
        courriel: info@dbpaysagiste.com
        url: https://dbpaysagiste.com
        lundi: 8h à 17h
        mardi: 8h à 17h
        mercredi: 8h à 17h
        jeudi: 8h à 17h
        vendredi: 8h à 17h
        samedi: fermé
        dimanche: fermé
        #https://itouchmap.com/latlong.html pour avoir les coordonnées lat/long
        latitude: 45.926713
        longitude: -74.138020
        facebookid: 285397091817919
        facebooklink: https://www.facebook.com/Dominique-Bernier-paysagiste-1426927820940903/
        googlepluslink: https://plus.google.com/+ViwebCa
        linkendinlink: https://www.linkedin.com/in/frédéric-thibault-272a9935/

# Doctrine Configuration
doctrine:
    dbal:
        driver: pdo_mysql
        host: '%database_host%'
        port: '%database_port%'
        dbname: '%database_name%'
        user: '%database_user%'
        password: '%database_password%'
        charset: UTF8
        unix_socket: "/Applications/MAMP/tmp/mysql/mysql.sock"
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: '%kernel.project_dir%/var/data/data.sqlite'
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #path: '%database_path%'

    orm:
        auto_generate_proxy_classes: '%kernel.debug%'
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: '%mailer_transport%'
    host: '%mailer_host%'
    username: '%mailer_user%'
    password: '%mailer_password%'
    spool: { type: memory }
    #port: 587
    #encryption: tls



fos_user:
   db_driver: orm
   firewall_name: main
   user_class: Viweb\UserBundle\Entity\User
   from_email:
       address: "%mailer_user%"
       sender_name: "%mailer_user%"
   registration:
       form:
           type: ViwebUserBundleFormRegistrationType
   profile:
       form:
           type: Viweb\UserBundle\Form\ProfilType

welp_mailchimp:
    api_key: c374e1368b84fa7f99d5cfa5e8d69c0f-us5